# Prompt: Screenshot to Flatsome UX Builder

Use the `flatsome-reference-rebuild` skill.

Analyze the provided website screenshots and rebuild a similar layout using WordPress Flatsome Theme and UX Builder shortcodes.

Screenshots provided:

- Desktop: {{DESKTOP_SCREENSHOT}}
- Tablet: {{TABLET_SCREENSHOT}}
- Mobile: {{MOBILE_SCREENSHOT}}

Rules:

- Do not copy the original brand identity.
- Do not copy text exactly.
- Do not copy images or icons.
- Recreate the layout as an inspired WordPress/Flatsome design.
- Use placeholders for all assets and text.
- Prefer `[section]`, `[row]`, `[col]`, `[ux_banner]`, `[text_box]`, `[featured_box]`, `[button]`, `[ux_products]`, `[accordion]`, and `[blog_posts]` where suitable.

Output:

1. Visual layout analysis
2. Flatsome shortcode
3. Custom CSS
4. Responsive notes
5. Replacement checklist
