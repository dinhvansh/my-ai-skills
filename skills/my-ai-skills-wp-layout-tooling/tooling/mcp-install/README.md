# MCP Install

This folder stores quick install commands for browser/page analysis tooling.

Start with:

```bash
codex mcp add playwright npx "@playwright/mcp@latest"
npm install -g firecrawl-cli
firecrawl setup mcp
firecrawl setup skills
```
