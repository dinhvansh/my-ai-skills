# Prompt: Screenshot To Flatsome Layout

Use the `flatsome-reference-rebuild` skill.

Input:

- Desktop screenshot: <attach or path>
- Mobile screenshot: <attach or path>

Task:

1. Analyze the visible layout.
2. Split into page sections.
3. Map each section to Flatsome elements.
4. Generate UX Builder shortcode.
5. Generate supporting CSS.
6. Add responsive notes.
7. Add checklist for replacing assets and validating the page.

Output using the standard skill format.
